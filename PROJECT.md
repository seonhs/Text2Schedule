# 1. 프로젝트 개요

- 프로젝트 명

 Text2Schedule

- 프로젝트 목적

 기관에서 발송되는 일정 안내 문자를 AI가 분석하여 캘린더에 등록할 수 있는 일정 정보로 자동 변환하는 프로그램이다.
교육 및 일정 안내 문자에는 날짜, 시간, 장소, 준비물, 안내사항 등 다양한 정보가 포함되어 있다.
사용자는 이러한 정보를 확인한 후 캘린더 앱을 실행하여 제목, 날짜, 시간, 장소, 메모 등을 직접 입력해야 한다.

 Text2Schedule은 이 과정을 AI를 통해 자동화하여 사용자가 문자 내용을 복사하고 확인하는 것만으로 일정 등록에 필요한 정보를 간편하게 생성할 수 있도록 하는 것을 목적으로 한다.

- 프로젝트 배경

 기관에서는 일정이나 출석 안내, 장소 변경, 준비사항 등의 정보를 문자 메시지로 전달하는 경우가 많다.
문자 메시지는 정보를 전달하는 데에는 편리하지만, 일정 관리 측면에서는 사용자가 필요한 정보를 직접 확인하고 캘린더에 다시 입력해야 한다는 불편함이 있다.
특히 여러 개의 교육 일정이 전달되거나 날짜와 시간, 장소, 준비사항 등이 복합적으로 포함된 경우 사용자가 필요한 정보를 구분하고 입력하는 데 추가적인 시간과 노력이 필요하다.
이에 따라 Text2Schedule은 문자 메시지를 일정 데이터로 변환하는 중간 과정을 AI가 담당하도록 하여 일정 관리의 편의성을 높이는 것을 목표로 한다.

- 해결하려는 문제

 현재 교육 안내 문자를 받은 후 캘린더에 일정을 등록하려면 다음과 같은 작업을 사용자가 직접 수행해야 한다.

문자 확인 -> 날짜 확인 -> 캘린더 앱 실행 -> 제목 입력 -> 날짜 입력 -> 시간 입력 -> 장소 입력 -> 메모 입력 -> 저장

이 과정에서 다음과 같은 문제가 발생할 수 있다.

문자에 포함된 일정 정보를 하나씩 확인해야 한다.
날짜와 시간, 장소 등을 직접 캘린더에 입력해야 한다.
준비물이나 안내사항을 메모에 다시 작성해야 한다.
여러 일정이 포함된 경우 반복적인 입력 작업이 발생한다.
사용자가 정보를 잘못 입력하거나 일부 내용을 누락할 가능성이 있다.

Text2Schedule은 이러한 문제를 다음과 같은 흐름으로 개선한다.

문자 복사 -> Text2Schedule 입력 -> AI 분석 -> 일정 정보 추출 -> 사용자 확인 및 수정 -> 일정 파일 생성 -> 캘린더 등록

- 프로젝트의 최종 목표

 Text2Schedule의 최종 목표는 '사용자가 실제로 받은 안내 문자를 입력하면 AI가 문자에서 일정에 필요한 정보를 자동으로 추출하고, 사용자가 내용을 확인·수정한 후 캘린더에 등록할 수 있는 형태로 변환하는 것'이다.
최종적으로 다음의 전체 과정을 구현하는 것을 목표로 한다.

안내 문자 내용 입력 -> AI 분석 -> 일정 정보 추출 -> 사용자 확인 및 수정 -> 일정 데이터 생성 -> .ics 일정 파일 생성 -> Android 삼성 캘린더 등록

MVP에서는 문자 → AI 분석 → 일정 정보 추출 → 사용자 확인 → 일정 파일 생성 → Android 삼성 캘린더 등록까지 정상적으로 처리되는 것을 핵심 성공 기준으로 한다.
궁극적으로는 사용자가 복잡한 일정 입력 과정을 직접 수행하지 않고도, 안내 문자를 캘린더 일정으로 빠르고 정확하게 변환할 수 있는 자동화 도구를 제공하는 것을 목표로 한다.

# 2. 주요 기능
## 2.1 문자 입력

사용자가 받은 문자 내용을 프로그렘에 붙여넣는다.
사용자 -> 문자 복사 -> Text2Schedule에 붙여넣기

## 2.2 일정 정보 추출

AI가 문자에서 캘린더 일정에 필요한 정보를 추출한다.
|항목|설명|
|---|---|
|SUMMARY|교육 / 프로그램 이름|
|DATE|일정 날짜|
|START_TIME|시작 시간|
|END_TIME|종료 시간|
|LOCATION|교육 장소|
|ORGANIZER|저장할 캘린더|
|RRULE|반복 일정 여부|
|DESCRIPTION|준비사항 / 주의사항|

## 2.3 일정과 메모 구분

 현재 버전에서는 별도의 리마인더 기능을 구현하지 않는다.
문자에 포함된 준비사항이나 주의사항은 일정의 memo에 통합한다.

예:
시간 엄수 -> 메모
주차 공간 협소 -> 메모
신청서 필요 -> 리마인더
개인 노트북 지참 -> 리마인더

## 2.4 사용자 확인 및 수정

 AI가 분석한 결과를 바로 캘린더에 저장하지 않는다.
사용자가 분석 결과를 확인하고 수정한 후 최종 저장하도록 한다.

예:
제목       스마트폰사진클래스
날짜       2026-08-19
시간       13:00 ~ 17:00
장소       광주청년일자리스테이션 용봉
메모       주차 공간 협소

[수정] [일정 추가]

최종 저장 권한은 사용자에게 있다.

## 2.5 .ics 일정 파일 생성

 사용자가 확인한 일정 정보를 .ics 형식으로 변환한다.

JSON -> ICS -> Android -> 삼성 캘린더

## 2.6 일정 변경 감지

 기존 일정과 새로운 안내문 사이에서 변경 가능성이 발견되면 사용자에게 표시한다.

예:

⚠ 일정 변경 가능성

기존 일정
14:30 ~ 17:30

새로운 안내
15:00 ~ 18:00

[기존 일정 수정]
[새 일정 추가]
[무시]

자동으로 기존 일정을 변경하지 않는다.

# 3. 일정 정보 정의
## 3.1 문자에서 필요한 정보 정의

|필드|의미|필수 여부|예시|
|...|...|...|...|
|SUMMARY|일정의 제목|필수|스마트폰사진클래스|
|DATE|일정 날짜|필수|2026-08-19|
|START_TIME|시작 시간|선택|13:00|
|END_TIME|종료 시간|선택|17:00|
|LOCATION|일정 장소|선택|청년일자리스테이션|
|ORGANIZER|저장할 캘린더|선택|1234@123.com|
|VALARM|알람 설정|선택|10분전|
|RRULE|반복 일정 여부|선택|매주|
|DESCRIPTION|안내사항,문의전화번호|선택|주차 공간이 협소함|

## 3.2 제목
교육이나 프로그램의 실제 일정명 및 제목 생성한다.
예:
[교육 안내]
스마트폰사진클래스 교육 참여가 확정되었습니다.

JSON
"SUMARY": "스마트폰사진클래스"

## 3.3 일정 날짜
실제 교육이 진행되는 아시아/서울 시간 기준으로 설정한다.
예:
일정: 8월 19알(수)

JSON
"DATE": "202060819"

연도가 없는 경우는 문자 수신일 기준 연도로 판단한다.
요일은 별도 데이터로 저장하지 않는다.

## 3.4 시작시간
예: 13:00 ~ 17:00 
시작 시간만 있는 경우 시작시간으로 부터 +1시간이 종료시간으로 판단합니다.
메모에 "종료시간 확인 필요" 작성

JSON
"START_TIME": 1300000
"END_TIME": 170000
오전/오후 표현도 표준 24시간 형시으로 변환한다.
향후 다른 시간대를 제공한다.

## 3.4 일정 장소
일정 장소를 Text로 저장한다.
예: 
장소: 청년일자리스테이션
JSOM
"LOCATION": "청년일자리스테이션"

주소가 함께 제공되는 경우에는 장소명과 주소를 함께 저장할지 결정해야 한다.
MVP에서는 장소명을 우선 사용하고, 정확한 위치 정보가 중요한 경우 주소를 포함하도록 하는 것이 좋다.
향후 Google map 혹은 naver map를 활용하여 위치 정보를 추가한다.

## 3.5 저장할 캘린더
일정을 저장할 클라우드 메일을 설정한다.

## 3.6 알람 설정
일정 날짜에 대한 리마인드 알람을 설정한다.

## 3.7 반복 일정
일정이 일정한 규칙에 따라 여러 차례 반복되는 경우 설정한다.
반복 주기가 명확한 경우 매일, 매주, 매월 등의 반복 규칙을 설정한다.
반복 주기가 일정하지 않거나 명확한 규칙으로 표현하기 어려운 경우에는 반복 일정으로 처리하지 않고 각 날짜를 개별 일정으로 등록하거나, 사용자의 확인을 요청한다.

## 3.8 메모
캘린더 일정에 참고할 가티가 있는 부가정보를 저장한다.
포함:
- 준비물
- 주의사항
- 안내사항
- 문의전화번호
- 주차안내
- 복장안내

예:
※ 현장 주차 공간이 매우 협소하며 유료로 운영됩니다.
※ 개인 노트북을 지참해 주세요.
JSOM
"DESCRIPTION": "현장 주차 공간이 매우 협소하며 유료로 운영됨.\n개인 노트북 지참."

# 4. 사용자 및 사용 환경

- 주요 사용자

 기관 및 행사에서 일정 안내 문자를 자주 받는 사용자

- 사용 목적

 문자로 받은 교육 및 프로그램 일정을 캘린더에 빠르게 등록

- 운영 환경

문자 내용 복사 -> Text2Schedule 실행 -> AI 분석 -> 사용자 확인 -> ICS 생성 -> 캘린더에 업로드

## MVP 개발 환경
Python 중심 개발
AI API 사용
삼성 캘린더를 기본 일정 저장 대상으로 사용

# 5. 개발 범위

## 5.1 MVP 포함 범위

- 문자 직접 입력
- 일정 정보 추출
- JSON 일정 데이터 생성
- 사용자 확인 및 수정
- .ics 파일 생성
- Android에서 일정 등록
- 삼성 캘린더 연동 검증
- 여러 일정 처리
- 일정 변경 가능성 표시
- 기본적인 예외 처리

## 5.2 MVP 제외 범위

 현재 프로젝트에서는 범위를 지나치게 넓히지 않기 위해 다음 기능을 제외한다.

- SMS 자동 수신 및 분석
- 생활패턴 및 일정 분석을 통한 알림 시간 추천
- 별도의 Android앱 개발

위 기능은 MVP 완성 이후 확장 대상으로 관리한다.

# 6. 기술 스택

|기술|역할|
|---|---|
|Python|프로그램 기본 언어|
|AI API|문자에서 일정 정보 추출|
|JSON|일정 데이터 저장 및 전달|
|ICS|캘린더 일정 파일 생성|
|Android|최종 일정 등록 환경|
|삼성 캘린더|일정 저장 대상|
|Git / GitHub|버전 관리|

## 캘린더 연동 방법

MVP에서는 .ics 파일 방식을 우선 검증한다.

Python -> .ics -> Android -> 삼성 캘린더

- .ics 방식의 장점
Python으로 구현하기 쉬움
표준적인 일정 교환 형식
MVP에 적합

- .ics 방식의 단점
Android에서 파일을 직접 열어야 할 수 있음
기기 및 환경에 따라 삼성 캘린더에서의 동작 확인 필요

따라서 개발 초기에는 Python에서 생성한 .ics 파일이 실제 Android 삼성 캘린더에서 정상적으로 등록되는지 먼저 검증한다.

## 향후 기술

전용 Android 앱으로 발전할 경우 Android Calendar Intent를 검토한다.

Python / AI -> 일정 데이터 -> Android -> Calendar Intent -> 삼성 캘린더 -> 사용자 확인 -> 저장

# 7. 프로젝트 구조

 초기 프로젝트는 Python 중심으로 구성한다.

Text2Schedule/
│
├── src/
│   ├── main.py
│   ├── parser.py
│   ├── ai_service.py
│   ├── calendar.py
│   └── validator.py
│
├── data/
│   └── samples/
│
├── tests/
│
├── docs/
│
├── .env
├── .gitignore
├── requirements.txt
├── README.md
└── PROJECT.md

## 주요 파일 역할

|파일|역할|
|---|---|
|main.py|프로그램 실행 및 전체 흐름 관리|
|parser.py|입력 문자 처리|
|ai_service.py|AI API 호출 및 일정 정보 추출|
|calendar.py|.ics 파일 생성|
|validator.py|일정 데이터 검증|
|data/samples/|테스트용 문자 데이터|
|tests/|테스트 코드|
|README.md|프로젝트 사용 방법|
|PROJECT.md|프로젝트 목표 및 개발 방향|
|.env|API Key 등 환경변수|
|.gitignore|Git에 업로드하지 않을 파일 관리|

# 8. 데이터 흐름
## 전체 데이터 흐름

안내 문자 -> Python 문자 입력 -> AI 분석 -> 일정 여부 판단 -> 일정 정보 추출 -> JSON 일정 데이터 -> 사용자 확인 / 수정 -> Python -> .ics 생성 -> Android -> 삼성 캘린더

## 일정 데이터 구조

AI가 최종적으로 만들어야 하는 기본 데이터는 다음과 같다.

{
    "title": "스마트폰사진클래스",
    "date": "2026-08-19",
    "start_time": "13:00",
    "end_time": "17:00",
    "location": "광주청년일자리스테이션 용봉",
    "repeat": null,
    "memo": "주차 공간이 협소함",
    "contact": null
}

## 데이터 처리 원칙

확실한 정보 -> 입력 -> 불확실한 정보 -> 사용자 확인 -> 없는 정보 -> 비워둠
AI가 알 수 없는 정보를 임의로 추측하지 않는다.

## AI의 역할

Text2Schedule의 AI는 단순한 문자 요약 AI가 아니다.

문자 -> 문자 내용 분석 -> 일정 여부 판단 -> 일정 정보 추출 -> 지정된 JSON 형식으로 반환

즉, 일정 정보 추출 AI에 가깝다.

## 실제 처리 예시

입력:

[확정 안내- 광주청년일자리스테이션 용봉]

<스마트폰사진클래스> 프로그램 참여가 최종 확정되었습니다.

* 일정: 8월 19일(수)
* 시간: 13:00 ~ 17:00
* 장소: 광주청년일자리스테이션 용봉
(광주 북구 용봉로 105, 5층 / 북구 행복어울림센터)

※ 현장 주차 공간이 매우 협소하며, 유료로 운영됩니다.

AI 처리 결과:

{
    "title": "스마트폰사진클래스",
    "date": "2026-08-19",
    "start_time": "13:00",
    "end_time": "17:00",
    "location": "광주청년일자리스테이션 용봉",
    "repeat": null,
    "memo": "현장 주차 공간이 매우 협소하며 유료로 운영됨.",
    "contact": null
}

-> 사용자 확인 -> .ics 생성 -> Android 삼성 캘린더 등록

# 9. 개발 원칙
## 9.1 실제 사용 과정 우선

기능을 많이 만드는 것보다 실제 사용 과정을 완성하는 것을 우선한다.

문자 입력 -> AI 분석 -> 일정 정보 추출 -> 사용자 확인 -> ICS 생성 -> Android -> 삼성 캘린더

## 8.2 사용자 최종 승인

AI가 분석한 일정을 자동으로 저장하지 않는다.

AI 분석 -> 사용자 확인 -> 사용자 수정 -> 사용자 승인 -> 일정 등록

최종 저장 권한은 사용자에게 있다.

## 9.3 추측하지 않기

AI가 문자에서 확인할 수 없는 정보를 임의로 생성하지 않는다.

확실한 정보 → 입력
불확실한 정보 → 사용자 확인
없는 정보 → 비워둠

## 9.4 단계적으로 개발

처음부터 SMS 자동 수신이나 Android 전용 앱을 개발하지 않는다.

캘린더 연동 검증 -> Python 기본 기능 -> JSON 데이터 구조 -> AI 일정 분석 -> 사용자 확인 -> ICS 생성 -> 실제 문자 테스트 -> 중복 / 변경 처리 -> Android 앱 확장

## 9.5 개인정보 보호

실제 문자에는 개인정보가 포함될 수 있으므로 불필요한 개인정보를 AI에 전달하지 않는다.

주의 대상:

전화번호
이메일
주민등록번호
개인 식별 정보

API Key는 코드에 직접 작성하지 않고 .env 파일을 사용한다.
.env는 .gitignore에 등록하여 GitHub에 업로드하지 않는다.
실제 문자 데이터 역시 GitHub에 그대로 업로드하지 않는다.

# 10. 단계별 개발 계획
## Phase 1 — 캘린더 연동 검증

목표:
Python -> test.ics -> Android -> 삼성 캘린더

검증 항목:
.ics 파일 생성
Android로 파일 전달
삼성 캘린더에서 파일 열기
일정 등록 가능 여부 확인
날짜 / 시간 / 장소 / 메모 정상 표시 확인
최종 요구되는 데이터 양식 구축

## Phase 2 — Python 기본 구조
프로젝트 생성
가상환경 구성
필수 라이브러리 설치
문자 입력
문자 저장
JSON 구조 정의
기본 .ics 생성

## Phase 3 — AI 일정 분석
AI API 연결
프롬프트 작성
일정 여부 판단
일정 정보 추출
JSON 출력
잘못된 데이터 검증

## Phase 4 — 사용자 확인
AI 분석 결과 표시
원문 확인
일정 정보 수정
일정 선택
최종 저장

## Phase 5 — 여러 일정 처리

예:

8월 18일
8월 19일
8월 20일

여러 날짜가 포함된 문자를 각각의 일정으로 처리한다.

## Phase 6 — 중복 및 일정 변경

기존 일정과 새로운 안내문을 비교하여 다음을 구분한다.

동일 일정
변경 가능성이 있는 일정
새로운 일정

자동으로 기존 일정을 변경하지 않고 사용자에게 선택권을 제공한다.

## Phase 7 — 실제 문자 테스트

실제로 받은 교육기관 문자를 활용하여 테스트한다.

테스트 대상:

교육 안내
교육 확정
일정 변경
여러 날짜가 포함된 문자
광고 문자
일정 정보가 부족한 문자

## Phase 8 — 개선 및 문서화
오류 수정
예외 처리
코드 정리
테스트 보완
README 작성
프로젝트 결과 정리

# 11. 테스트 기준
|테스트 상황|기대 결과|
|...|...|
|정상적인 교육 문자|일정 생성|
|날짜가 없는 문자|사용자에게 확인 요청|
|시간이 없는 문자|사용자에게 확인 요청|
|장소가 없는 문자|빈 값 또는 사용자 확인|
|여러 날짜가 포함된 문자|각각의 일정 생성|
|일정 변경 문자|변경 가능성 표시|
|사용자가 수정한 일정|수정된 내용으로 ICS 생성|
|불확실한 정보|임의 추측하지 않음|
|기존 일정과 동일한 문자|중복 여부 표시 또는 처리|
|기존 일정과 다른 시간	일정 변경|가능성 표시|

# 12. 현재 개발 상태
# Phase 1 — 캘린더 연동 검증

- [o] Python에서 .ics 생성
- [o] 테스트 .ics 파일 생성
- [o] Android로 파일 전달
- [o] 삼성 캘린더에서 파일 열기
- [o] 일정 등록 확인
- [o] 날짜 / 시간 / 장소 / 메모 확인

## Phase 2 — Python 기본 구조
- [] 프로젝트 구조 생성
- [] 가상환경 구성
- [] requirements.txt 작성
- [] 문자 입력 기능
- [] JSON 데이터 구조
- [] .ics 생성 모듈

## Phase 3 — AI
- [] AI API 연결
- [] 프롬프트 작성
- [] 일정 정보 추출
- [] JSON 출력
- [] 데이터 검증

## Phase 4 — 사용자 확인
- [] AI 결과 화면
- [] 원문 확인
- [] 일정 수정
- [] 일정 선택
- [] 최종 저장

## hase 5 — 고급 기능

- [] 여러 일정 처리
- [] 중복 일정 처리
- [] 일정 변경 감지
- [] 실제 문자 테스트

## Phase 6 — 마무리

- [] 예외 처리
- [] 코드 리팩토링
- [] 테스트
- [] README 작성
- [] 프로젝트 결과 정리

# 13. 향후 개선 사항
Android 연동 확대

현재:

문자 복사 -> Text2Schedule 

향후:

문자 복사 -> Android 공유 기능 -> Text2Schedule

## SMS 자동 처리

향후 SMS 자동 수신 및 분석 기능을 검토한다.
SMS 수신 -> 자동 분석 -> 사용자 확인 -> 캘린더 등록

## 리마인더 기능

일정과 별도로 준비사항이나 알림을 관리할 수 있도록 확장한다.

## 전용 Android 앱

MVP 완성 이후 전용 Android 앱 개발을 검토한다.
최종적으로는 다음과 같은 형태로 발전시킬 수 있다.

SMS / 문자 -> Text2Schedule Android 앱 -> AI 분석 -> 사용자 확인 -> 삼성 캘린더

# 14. 변경 이력
날짜	버전	변경 내용
2026-08-14	v0.1.0	Text2Schedule 프로젝트 기본 기획 및 설계 작성
2026-08-27 v0.1.0 SMS 분석 기능 삭제 및 세부 내용 변경
2026-08-29 v0.1.0 수집 데이터 정의 추가
2026-08-31 v0.1.0 수집 데이터 정의 수정
